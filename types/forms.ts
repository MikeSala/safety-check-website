export type WorkFormValues = {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  emailAddress: string;
  fileName: string;
  fileContent: string;
};

export type ContactFormValues = {
  name: string;
  phone: string;
  email: string;
  subject: string;
  message: string;
};
